import XCTest
@testable import Viewity

final class ViewityTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(Viewity().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
